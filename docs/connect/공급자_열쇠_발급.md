# 연결 서비스 열쇠 발급표 (대표가 그대로 따라 하는 판)

작성 2026-09-30. 화면 = 큐리AI `/os/connect` 「서비스」 탭.

## 0. 먼저 알 것

- **누구 계정이 붙나** = 큐리AI 사용자 본인의 계정. 우리 회사 계정을 붙이는 게 아니다. 사용자가 「연결하기」를 누르면 그 서비스 로그인 화면으로 가서 「허용」을 누른다.
- **대표가 할 일** = 서비스마다 개발자 콘솔에서 「앱」 하나를 만들고, 거기서 나오는 열쇠 2개(아이디, 비밀)를 Vercel 환경변수에 넣는다. 열쇠가 없는 서비스는 화면에 「준비 중」으로 나오고 단추가 눌리지 않는다.
- **돌아오는 주소(리디렉트 URI)** 는 글자 하나까지 같아야 한다. 아래 표의 값을 콘솔에 그대로 붙인다. 시험용은 `http://localhost:3000` 으로 바꿔 하나 더 넣는다.
- 앱 이름은 전부 **큐리AI** 로 한다(콘솔이 영어만 받으면 `CuriAI`).
- 공통 자물쇠 `CONNECTOR_SECRET_KEY` 가 먼저 있어야 한다(이미 있음. 없으면 14개 전부 「준비 중」).

## 1. 발급표

| 서비스 | 콘솔 주소 | 만들 것 | 돌아오는 주소(콘솔에 붙일 값) | 켤 권한 | 환경변수 2개 |
|---|---|---|---|---|---|
| 노션 | https://www.notion.so/my-integrations | 「새 통합」 → 유형 **공개(Public)** | `https://curi-ai.vercel.app/api/connect/notion/callback` | 콘텐츠 읽기만(쓰기·삭제 끄기) | `NOTION_CLIENT_ID` `NOTION_CLIENT_SECRET` |
| 슬랙 | https://api.slack.com/apps | 「Create New App」 → From scratch | `https://curi-ai.vercel.app/api/connect/slack/callback` | Bot Token Scopes: `channels:read` `channels:history` `chat:write` | `SLACK_CLIENT_ID` `SLACK_CLIENT_SECRET` |
| 카카오톡 | https://developers.kakao.com/console/app | 「애플리케이션 추가」 → 카카오 로그인 켬 → 보안에서 **Client Secret 생성·사용** | `https://curi-ai.vercel.app/api/connect/kakao/callback` | 동의항목: 닉네임(profile_nickname), 카카오톡 메시지 전송(talk_message) | `KAKAO_REST_API_KEY` `KAKAO_CLIENT_SECRET` |
| 지메일 | https://console.cloud.google.com/apis/credentials | 프로젝트 하나 → 「OAuth 클라이언트 ID」 → 웹 애플리케이션. Gmail API, Calendar API, YouTube Data API v3 켬 | `https://curi-ai.vercel.app/api/connect/gmail/callback` | `gmail.readonly` (+ `openid` `email`) | `GOOGLE_OAUTH_CLIENT_ID` `GOOGLE_OAUTH_CLIENT_SECRET` |
| 구글 캘린더 | (지메일과 같은 앱) | 같은 클라이언트에 주소만 하나 더 | `https://curi-ai.vercel.app/api/connect/google_calendar/callback` | `calendar.readonly` | (지메일과 같음) |
| 유튜브 | (지메일과 같은 앱) | 같은 클라이언트에 주소만 하나 더 | `https://curi-ai.vercel.app/api/connect/youtube/callback` | `youtube.readonly` | (지메일과 같음) |
| 구글 드라이브 | (지메일과 같은 앱) | 같은 클라이언트에 주소만 하나 더. 콘솔에서 **Google Drive API** 를 켜야 한다(2026-10-01, 갈래 G) | `https://curi-ai.vercel.app/api/connect/google_drive/callback` | `drive.readonly` | (지메일과 같음) |
| 네이버 캘린더 | https://developers.naver.com/apps | 「애플리케이션 등록」 → 사용 API: 네이버 로그인 + **캘린더** | `https://curi-ai.vercel.app/api/connect/naver_calendar/callback` | 캘린더(읽기), 회원이름·이메일 | `NAVER_CLIENT_ID` `NAVER_CLIENT_SECRET` |
| 네이버 블로그 | (네이버 캘린더와 같은 앱) | 사용 API 에 **블로그** 추가, 주소 하나 더 | `https://curi-ai.vercel.app/api/connect/naver_blog/callback` | 블로그 | (네이버 캘린더와 같음) |
| Zoom | https://marketplace.zoom.us/develop/create | 「General App」 → User-managed | `https://curi-ai.vercel.app/api/connect/zoom/callback` | Scopes: `user:read:user` `meeting:read:list_meetings` `cloud_recording:read:list_user_recordings` | `ZOOM_CLIENT_ID` `ZOOM_CLIENT_SECRET` |
| 스레드 | https://developers.facebook.com/apps | 앱 만들기 → 「Threads API」 사용 사례 추가 | `https://curi-ai.vercel.app/api/connect/threads/callback` | `threads_basic` | `THREADS_APP_ID` `THREADS_APP_SECRET` |
| 인스타그램 | https://developers.facebook.com/apps | 앱 만들기 → 「Instagram」 → Instagram 로그인(비즈니스) | `https://curi-ai.vercel.app/api/connect/instagram/callback` | `instagram_business_basic` (옛 `instagram_basic` 은 2024-12 에 닫힘) | `INSTAGRAM_APP_ID` `INSTAGRAM_APP_SECRET` |
| 깃허브 | https://github.com/settings/developers | 「OAuth Apps」 → New OAuth App | `https://curi-ai.vercel.app/api/connect/github/callback` | `read:user` (비공개 저장소 읽기만 권한은 깃허브에 없다. `repo` 는 쓰기까지 열려서 안 받는다) | `GITHUB_CLIENT_ID` `GITHUB_CLIENT_SECRET` |
| 큐리어스 | (우리 본체) | 본체(curious-500.com)에 로그인 토큰 창구를 열어야 한다. 성연 손 | 없음(준비 중) | 없음 | 없음(열쇠가 있어도 아직 열리지 않는다) |

실제 서비스 주소가 `curi-ai.vercel.app` 이 아니면 그 주소로 바꿔 넣는다. 우리 쪽은 `NEXT_PUBLIC_APP_URL` 로 같은 주소를 만든다.

## 2. 넣는 곳

Vercel → 프로젝트 → Settings → Environment Variables. Production 에 넣고 다시 배포하면 그 서비스의 「준비 중」 배지가 사라지고 「연결하기」가 눌린다. (⛔ 도구 규칙이 Vercel 환경변수 쓰기를 막아서 이건 대표 손이다.)

## 3. 심사·시간

| 서비스 | 심사 | 걸리는 시간 |
|---|---|---|
| 노션 공개 통합 | 있음(간단) | 발급 즉시 시험 가능, 다른 사용자가 붙이려면 심사 1~3일 |
| 구글 4종 | 있음. 시험 사용자 100명까지는 심사 없이 됨 | 즉시(시험 사용자). 공개는 앱 인증 2~6주 |
| 메타 2종(스레드, 인스타) | 있음. 개발 모드에서는 역할 등록한 계정만 | 즉시(개발 모드). 공개는 앱 리뷰 1~4주 |
| 카카오, 네이버, Zoom, 깃허브, 슬랙 | 없음(카카오·네이버는 검수 없이도 본인 계정은 됨) | 즉시 |

## 4. 어디에 무엇이 저장되나

- 토큰 JSON 은 `connectors.secret_encrypted` 에 **잠겨서**(AES-256-GCM) 들어간다. 화면과 응답에는 계정 힌트(`jin@…`)만 나간다.
- 연결 해제 = 그 줄을 지운다. 서비스 쪽 「앱 연결 끊기」는 사용자가 그 서비스 설정에서 따로 할 수 있다.
