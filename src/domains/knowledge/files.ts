/**
 * 「나를 닮은 AI」에 올릴 수 있는 파일 — 여기가 유일한 목록이다
 *
 * 2026-09-17 대표 지적 「AI 만들기에서 파일학습이 안되네. 엑셀파일 등등」
 * 고치면서 보니 같은 목록이 세 군데(화면·업로드 창구 둘)에 손으로 적혀 있었다.
 * 한 곳만 고치면 나머지가 막아서 「왜 안 되지」가 된다. 그래서 한 표로 모은다.
 *
 * 서버가 실제로 글자를 뽑을 수 있는 것만 넣는다(process 창구와 짝이 맞아야 한다).
 */
export const 올릴수있는파일 = [
    'pdf',
    'txt', 'md',
    'doc', 'docx',
    'hwp', 'hwpx',
    'ppt', 'pptx',
    'xlsx', 'xls', 'csv',   // 2026-09-17 추가
    'vtt',                  // 줌 녹화 자막
] as const

/** 파일 고르는 칸에 넣는 글 (accept="...") */
export const 고르기필터 = 올릴수있는파일.map((e) => '.' + e).join(',')

/** 사람에게 보여줄 안내 */
export const 안내문구 = 'PDF · 워드 · 한글 · 엑셀 · 파워포인트 · 텍스트'
